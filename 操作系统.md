# 从敲下 `ls` 到屏幕显示：我亲手拆解了计算机的“执行链”

换 Mac 之后，我本来只担心一个问题：Ubuntu 默认是 Bash，Mac 默认是 Zsh，这俩差别大不大？会不会像 PowerShell 那样把 `ls` 都改成 `Get-ChildItem`？后来发现差别不大，但我却因此陷入了一个更深的困惑——我根本说不清楚，终端里敲下 `ls` 然后回车，这中间到底发生了什么。

我决定顺着这条链子往下挖，结果挖穿了整个计算机的执行体系。

先说 Shell 是什么。终端里那个等待输入的提示符就是 Shell，它可能叫 Bash，也可能叫 Zsh。但它的本质是一个普通的用户态进程，和 Chrome、VSCode、`ls` 没有区别，都是被内核加载进内存、由 CPU 逐条执行机器码的程序。只不过它的本职工作是打印提示符、读取键盘输入、解析字符串。

当我敲 `cd` 时，Shell 自己就处理了，因为 `cd` 是内置命令，它的机器码就在 Shell 自己的代码段里。但当我敲 `ls` 时，事情就变了——`ls` 是一个存放在硬盘上的独立可执行文件。Shell 需要找到它，并想办法让它跑起来。而 Shell 自己没法让 `ls` 跑起来，它必须求助内核。

这里涉及 CPU 的两种工作模式。x86 架构下叫 Ring 0 和 Ring 3，也叫内核态和用户态。内核态拥有最高权限，能执行所有 CPU 指令，能直接访问物理内存和硬件设备。用户态则受限，不能碰硬件，不能访问其他进程的内存。操作系统内核运行在 Ring 0，而 Shell、`ls`、Chrome 全都运行在 Ring 3。

用户态程序需要做“越界”事情时，必须通过一个特殊机制把控制权交给内核。这个机制叫系统调用，在 x86-64 上由一条 `syscall` 指令触发。

`ls` 是怎么被启动的？Shell 先确认你敲的是 `ls`，然后开始准备调用 `execve`——这是用来加载和执行程序的内核函数。在 Linux 里 `execve` 的系统调用号是 59。Shell 把 59 写入 `rax` 寄存器，把字符串 `"/bin/ls"` 的内存地址写入 `rdi` 寄存器，然后把参数数组地址写入 `rsi`，环境变量地址写入 `rdx`。做完这些，Shell 执行 `syscall` 指令。

`syscall` 这条指令很特殊，它不在用户程序自己的代码段里做任何事，而是直接告诉 CPU 的硬件逻辑：把当前的权限从 Ring 3 提升到 Ring 0，然后把 PC 寄存器强制指向一个预先设置好的内核入口地址。这个入口地址是操作系统在开机初始化时就写进 CPU 的专用寄存器（MSR）里的，不随进程切换而改变。

注意，在执行 `syscall` 的过程中，`rax`、`rdi`、`rsi`、`rdx` 这些通用寄存器的值被完整地带进了内核态。内核的第一件事就是检查 `rax` 里的数字——发现是 59，于是知道这次系统调用要执行的是 `execve`。接着内核从 `rdi` 拿到地址，去 Shell 的进程空间里把字符串 `"/bin/ls"` 读出来，再去文件系统里找这个文件。

如果只讲到这里，你可能觉得已经够了。但真正让我彻底理解计算机运作的，是理解了 PC 寄存器——程序计数器。

CPU 内部有一个叫 PC（Program Counter）的寄存器，也叫指令指针。它里面存着一个内存地址。CPU 通电后的唯一工作就是：看 PC 指向哪里，就去那个内存地址取一条机器码指令，然后执行它，再把 PC 自动加一指向下一条指令，如此反复。

改变 PC 寄存器的值，就是改变 CPU 正在执行的代码流。有四种方式可以改变 PC：

第一种是顺序执行。CPU 取完一条指令后，硬件自动把 PC 加上当前指令的长度，指向下一条相邻的指令。这是默认行为。

第二种是跳转指令。像 `JMP`、`CALL` 这样的指令，执行时 CPU 直接把指令里携带的目标地址复制进 PC。这就是函数调用和循环的实现基础。

第三种是系统调用。执行 `syscall` 指令时，CPU 硬件不是从指令里取地址，而是从内部专用寄存器（MSR）里读出内核入口地址，写入 PC。

第四种是硬件中断。外部设备给 CPU 引脚发送电信号，CPU 在执行完当前指令后检查到中断信号，暂停顺序执行，去内存中的中断描述符表里查表，把对应的处理程序地址加载进 PC。

键盘输入就是第四种。我敲 `l` 时，键盘控制器向 CPU 发送中断信号，CPU 被硬拽进内核的中断处理程序。内核程序把按键的扫描码翻译成 ASCII 码，存进 Shell 进程的输入缓冲区，然后执行中断返回指令，把 PC 恢复成被打断前的地址，CPU 继续执行刚才被中断的任务。整个过程只是“存数据”，没有任何“启动程序”的动作。敲 `s` 和回车也一样。直到 Shell 的 `read()` 系统调用发现缓冲区里收到了换行符，才把 `"ls"` 这个字符串交给解析逻辑。

回车后，Shell 执行的是早已写在内存里的一组条件判断机器码——`cmp` 指令比较缓冲区内容和预置字符串，`jne` 指令根据比较结果决定跳转还是顺序执行。如果缓冲区里是 `"cd"`，CPU 跳转到处理 `cd` 的代码段，绕过了后面的 `syscall`；如果缓冲区里是 `"ls"`，条件不满足，CPU 不跳转，PC 顺序走到下一条指令——那条指令恰好就是 `syscall`。

所以，我敲 `ls` 并不是“触发”了一条新指令的生成，那串 `syscall` 的二进制代码早在 Shell 编译时就写死在硬盘里了，加载进内存后从未改变过。我敲的只是数据，这些数据通过条件判断影响了 CPU 的“选择”——是走入 `syscall` 分支，还是走入其他分支。

回到内核。它拿到 `"/bin/ls"` 这个路径后，去硬盘读取文件，解析 ELF 或 Mach-O 格式头，确认是可执行文件。然后在物理内存中找一块空闲区域，把 `ls` 的机器码从硬盘拷贝进去。接下来是关键一步：内核在进程的页表里建立虚拟地址到物理地址的映射关系，并把 `ls` 的入口虚拟地址写入子进程的进程控制块中保存的 PC 值。做完这一切，内核执行 `sysret` 指令返回用户态，CPU 硬件把之前存好的入口地址加载进真正的 PC 寄存器。从此，CPU 开始逐条执行 `ls` 的机器码。

但这里有一个极其容易被误解的地方：**启动 `ls` 需要内核参与，但启动之后，CPU 绝大多数时间根本不鸟内核**。

`ls` 的机器码被 CPU 执行后，它要做的是解析命令行参数、读取目录内容、对文件名排序、计算文件大小显示格式、处理颜色输出——这些全都是纯粹的算术运算、字符串比较、逻辑判断和内存搬运。这些操作对应的指令是 `ADD`、`SUB`、`CMP`、`MOV`、`JMP`，全部能在用户态由 CPU 自己完成，不需要调用内核。PC 一直停留在 `ls` 的代码段，一条接一条地顺序前进，只有在遇到 `write` 系统调用（往屏幕输出结果）时，才再次触发 `syscall` 跳进内核。

这就是用户态和内核态的分工边界：**当 CPU 执行的那条机器码只需要处理进程自己的数据（加减乘除、比较、排序、在自己内存里搬数据），就在用户态自娱自乐；一旦操作涉及到其他进程、硬盘、网卡、屏幕等共享资源，就必须通过 `syscall` 或硬件中断把控制权交给内核。**

这个边界之所以能存在，靠的是虚拟内存机制。用户程序看到的内存地址是连续的虚拟地址，从 `0x0000` 开始，但它对应的物理内存可能分散在各处，甚至被交换到了硬盘上。地址翻译由 CPU 内部的 MMU（内存管理单元）硬件完成。内核提前在内存中建好页表，把页表基址写入 CPU 的 `cr3` 寄存器。之后每次内存访问，MMU 自动查页表翻译地址，全程不执行任何内核机器码。只有发生缺页异常——也就是 MMU 查表发现该页不在物理内存中时——才会触发异常，把 PC 硬拽进内核的缺页处理程序。

时钟中断则是内核实现进程调度的基石。主板上有个时钟芯片，每隔几毫秒向 CPU 发送一次中断信号。CPU 被硬拽进内核的时钟中断处理程序。这段代码检查当前进程的时间片是否用完，如果用完，就把当前进程的 PC 值存进内存里的进程控制块，再从就绪队列里选一个新进程，把它的 PC 值写入 CPU。然后中断返回，CPU 继续执行，但执行的已经是另一个进程的代码了。通过这个机制，内核让所有进程都以为自己独占了 CPU。

回过头看内核到底是什么。它是一段常驻内存的、拥有最高权限的机器码集合。它不主动做任何事，只是躺在那里等 CPU 通过三种方式把 PC 指向它：`syscall` 指令、硬件中断、CPU 异常。它唯一且终极的工作，是维护物理资源（CPU 时间、物理内存、硬盘）的全局状态，并严格监管页表（`cr3` 寄存器）和程序执行流（PC 寄存器）的切换。

现在我可以清晰地回答最初的问题了：我敲下 `ls` 然后回车，完整的执行链是这样的。

我敲下键盘，硬件中断把 PC 拽进内核，内核把按键字符存进 Shell 缓冲区，然后恢复 PC。敲回车后，Shell 解析字符串，发现是 `ls`，于是把 `/bin/ls` 的地址和系统调用号 59 填入寄存器，执行 `syscall` 指令。CPU 硬件把 PC 指向内核入口，带着寄存器里的参数进入内核态。内核读寄存器、读 Shell 内存里的路径字符串、去硬盘找文件、分配物理内存、建立页表映射、拷贝机器码、把 `ls` 的入口地址写入子进程的 PC。内核执行 `sysret` 返回，CPU 的 PC 正式指向 `ls` 的第一条机器码。然后 CPU 在用户态一条接一条地执行 `ls` 的排序、格式化、计算逻辑，完全不经过内核，直到最后要输出到屏幕才再次触发 `syscall`。

最终呈现出来的，就是屏幕上那几行文件名。

这条链走完，我发现自己不再担心 Shell 的版本差异了。无论是 Bash 还是 Zsh，都只是内存里的一段普通机器码，它们做的事情是相同的——等待输入、解析字符串、通过 `syscall` 请求内核加载外部程序。区别只是语法糖和交互体验，底层的执行逻辑从来没有变过。更关键的是，我从此能用 PC 寄存器这个导航仪去理解任何计算机程序的执行过程了。