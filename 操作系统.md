# 从敲下 `ls` 到文件名出现在屏幕上：一条更真实的执行链

换到 Mac 之后，我最先注意到的是 Shell 变了：Ubuntu 常见的是 Bash，macOS 默认的是 Zsh。[^mac-shell] 它们都能执行 `ls`，但这件看似简单的事背后，其实牵涉终端模拟器、Shell、系统调用、虚拟内存、文件系统和进程调度。

我一开始试图用一句话概括这个过程：Shell 收到 `ls`，调用内核加载程序，然后 CPU 开始执行。这个说法不能算完全错，但它省略了几个最关键的环节，也很容易让人形成错误的心智模型。

下面我重新走一遍这条链。

先限定讨论范围：**正文以 x86-64 Linux、GUI 终端模拟器和交互式 Shell 为例**。Ubuntu 上常见的 Bash、GNU Coreutils `ls` 可以放进这个模型。macOS 的高层流程相似，但它使用 Darwin/XNU；Apple Silicon 是 ARM64，原生可执行文件是 Mach-O，系统调用指令、寄存器约定和可执行文件加载细节都与本文不同。[^apple-silicon] [^macho]

## 第一站不是 Shell，而是终端模拟器

打开 Terminal、GNOME Terminal、Konsole 或其他终端应用时，屏幕上的窗口首先属于一个**终端模拟器进程**。Shell 是这个终端模拟器启动的另一个用户态进程。

两者通常通过一对伪终端，也就是 PTY，相连：

```text
键盘事件 -> 终端模拟器 -> PTY master -> PTY slave -> Shell
Shell/ls -> PTY slave -> PTY master -> 终端模拟器 -> 屏幕
```

PTY 的 master 端由终端模拟器持有，slave 端则表现得像一台传统终端。Shell 的标准输入、标准输出和标准错误通常都连接到这个 slave 端。Linux 内核负责连接 PTY 两端，并在其中实现 TTY 层和行规程。[^pty] [^tty-ldisc]

因此，当我按下字母 `l` 时，不能简单地说“键盘中断把 ASCII 字符写进了 Shell 的缓冲区”。更准确的说法是：硬件和操作系统输入栈先产生按键事件，GUI 终端模拟器收到事件，再结合键盘布局、修饰键和输入法生成一个或多个字节，并把它们写入 PTY master。Shell 随后从 PTY slave 读取这些字节。

这一区别很重要。键盘输入不一定是 ASCII，一个按键也不一定只产生一个字节；方向键通常会产生转义序列，中文输入法更不可能用“扫描码直接转 ASCII”来解释。

交互式 Shell 还会使用 Readline、ZLE 一类行编辑器。它们为了支持光标移动、历史记录和补全，可以逐字符读取和处理输入。只有终端处于 canonical mode 时，内核的终端接口才通常以换行为界向读取者交付完整的一行；不能笼统地说 Shell 的 `read()` 一定等到回车才看到输入。[^posix-terminal] [^readline]

当我按下回车，行编辑器接受当前命令行，Shell 才开始处理字符串 `ls`。

## Shell 先解析，再决定执行什么

Shell 并不是拿输入和几个预置字符串做一次 `cmp`。一条命令可能包含引号、变量展开、通配符、管道、重定向、命令替换和控制运算符，因此 Shell 需要进行词法分析、语法解析和各种展开。

对于一个简单命令，Shell 还要判断命令名究竟是什么：它可能是 alias、Shell 函数、builtin，也可能是需要从 `$PATH` 中寻找的外部可执行文件。Bash 对函数、builtin 和 `$PATH` 的查找顺序有明确规则，并会缓存已经找到的路径。[^bash-search]

这也解释了为什么不能一看到 `ls` 就断言它必然是 `/bin/ls`。可以用下面的命令询问当前 Shell：

```sh
type ls
command -V ls
```

`cd` 通常必须是 builtin，关键原因也不是“它的机器码恰好编译在 Shell 里”，而是它需要改变**当前 Shell 进程**的工作目录。如果另起一个普通子进程执行 `cd`，改变的只会是子进程自己的工作目录；子进程退出后，父 Shell 不会跟着改变。

假设这里没有 alias 或函数干扰，`ls` 最终被解析为一个外部程序。Shell 接下来要给它建立独立的执行环境，包括参数、环境变量、文件描述符、当前目录以及重定向结果。Bash 文档把它称为 separate execution environment。[^bash-env]

## 创建进程和执行程序不是同一件事

在 Unix 模型中，最常见的做法是 Shell 先创建子进程，然后由子进程执行新程序。经典接口组合是：

```text
父 Shell --fork/spawn--> 子进程 --execve--> ls 程序映像
          \
           `--等待或进行作业控制
```

具体 Shell 和 C 库可以使用 `fork`、`vfork`、`clone` 或 `posix_spawn` 一类机制，也可能做实现层面的优化，但有一个概念必须分清：**创建进程和替换程序映像是两件不同的事。**

`fork` 创建子进程；`execve` 不创建新进程，而是把调用进程当前正在执行的程序替换成另一个程序。成功执行 `execve` 后，进程仍然保留自己的 PID 和一部分进程属性，但原来的代码、数据、堆栈等程序映像会被新程序替换。成功的 `execve` 不会返回到原来的调用点。[^fork] [^execve]

这也是交互式 Shell 通常不能直接把自己 `execve` 成 `ls` 的原因：那样 Shell 本身就消失了，`ls` 结束后也没有原来的 Shell 可以重新打印提示符。Shell 的 `exec` builtin 则正是有意要求这种“用新程序替换当前 Shell”的行为。

## `syscall` 到底做了什么

普通应用通常调用 libc 提供的 `execve()` 包装函数，而不是亲手填写寄存器。继续限定在 Linux x86-64：原生系统调用 ABI 把系统调用号放在 `rax`，前三个参数放在 `rdi`、`rsi`、`rdx`；`execve` 的系统调用号是 59。随后，底层包装代码执行 `syscall` 指令。[^linux-syscall-table]

这里的 59 和寄存器安排只是 **Linux x86-64 ABI** 的细节，不是 `execve` 的跨平台定义。换成 Linux ARM64、macOS ARM64 或其他系统，数字、寄存器和进入内核的指令都可能不同。

在 x86-64 上，CPU 执行 `syscall` 时，会按照模型专用寄存器中预先配置的信息转移到内核入口，并改变相应的执行权限和状态。CPU 还会把返回地址和标志保存到规定的寄存器；例如 `RCX` 和 `R11` 会被该指令占用，因此不能说所有用户态寄存器都被“原样、完整地保留”。切换到内核栈、保存其余上下文、检查系统调用号和安全地读取用户指针，还需要内核入口代码继续完成。具体指令语义由 Intel 架构手册定义。[^intel-sdm]

内核拿到的路径、参数数组和环境变量数组都只是用户空间地址。它不能无条件信任这些指针，而要进行边界与权限检查，并通过受控方式访问对应内存。

## `execve` 不是把整个文件搬进连续内存

找到可执行文件后，内核需要检查路径、执行权限和文件格式。对常见的动态链接 ELF 程序，它会根据 ELF program headers 建立新的虚拟内存布局，设置代码段、数据段、栈、参数、环境变量和 auxiliary vector，并处理 ELF 指定的解释器，也就是动态加载器。

这里最容易产生的误解是“内核找一块连续物理内存，把 `ls` 的全部机器码从硬盘复制进去”。现代虚拟内存并不是这样工作的。

内核主要建立虚拟内存区域和映射关系。可执行文件的页面可以由文件和页缓存支持，并在真正访问时通过 demand paging 进入物理内存。不同虚拟页对应的物理页可以分散在各处；文件内容也可能已经在页缓存里，根本不需要再次读取物理磁盘。Linux 内核文档明确把 demand paging、文件映射和页缓存列为虚拟内存系统的组成部分。[^mm-concepts]

如果 ELF 使用动态链接，最先运行的用户态代码通常包括动态加载器。它负责装载所需共享库、完成重定位，然后才把控制权交给程序的启动代码，最终进入 `main`。因此，“CPU 从此直接执行 `ls` 的第一条业务指令”仍然省略了重要阶段。

成功的 `execve` 不会回到子进程原先的 libc 调用点。内核的返回用户态路径会恢复为新程序准备好的执行上下文，并从新映像的入口继续运行。至于某一次具体返回最终使用 `SYSRET` 还是 `IRET` 等路径，属于内核和架构实现细节，不应写成应用程序可以依赖的固定步骤。

## `ls` 运行时会多次进入内核

进入 `ls` 之后，确实有大量工作可以留在用户态完成，例如解析参数、比较字符串、排序目录项和格式化输出。但“只有最后输出时才需要内核”是错误的，因为 `ls` 的核心任务正是读取文件系统数据。

在 Linux 上，一次普通的目录列表通常会涉及这些类别的操作：

1. 打开目标目录。
2. 通过 `getdents64` 一类系统调用取得目录项。
3. 根据选项、文件系统能力和颜色规则，使用 `stat`/`statx` 一类接口取得额外元数据。
4. 在用户态排序并格式化结果。
5. 通过 `write` 把结果写到标准输出。
6. 关闭资源并退出进程。

`getdents64` 的职责就是从一个目录文件描述符中读取目录项。[^getdents] GNU `ls` 的文档也特别指出，某些颜色规则会导致它对列出的文件执行额外的 `stat`，这会产生明显开销。[^ls-color]

此外，动态加载、内存分配、缺页异常、信号和调度都可能让执行流进入内核。`ls` 在用户态和内核态之间通常会往返多次，而不是启动时进入一次、结束时再进入一次。

## `write` 写出的也不是像素

当 `ls` 调用 `write` 时，它只是向自己的标准输出文件描述符写入字节。如果标准输出连接着终端，这些字节会进入 PTY slave，并从 master 端交给终端模拟器。

终端模拟器随后解析普通字符和 ANSI 转义序列，维护光标、颜色和屏幕缓冲区，再通过字体与图形系统把字符渲染成像素。只有走完这一步，文件名才真正出现在显示器上。

如果执行的是：

```sh
ls > files.txt
```

同样的 `ls` 不会把结果送到终端模拟器，而是写进文件。这进一步说明 `write` 的语义是“向文件描述符写字节”，不是“往屏幕输出”。

## 用户态与内核态的边界究竟在哪里

一个更可靠的判断标准不是“数据是不是自己的”或者“资源是不是共享的”，而是：当前指令是否有权访问目标地址、执行目标操作，以及是否需要内核提供受保护的操作系统服务。

用户态程序可以直接读写已经映射且权限允许的内存，其中甚至可以包含与其他进程共享的内存。反过来，一个看似只查询自身信息的操作也可能通过系统调用完成。系统调用是用户程序主动请求内核服务的接口；硬件中断则通常是异步事件，不能把二者并列成用户程序操作资源的两种选择。

虚拟内存参与了隔离，但它不是全部。CPU 的特权级、页表权限、内核的对象与权限检查，以及某些系统上的 IOMMU 等机制共同建立了边界。

进程的虚拟地址空间也不是“从 `0x0000` 开始连续可用的一大块内存”。它通常包含许多映射和空洞，零地址附近还经常故意保持未映射，以便捕获空指针访问。

CPU 会使用 TLB 缓存近期的地址翻译；只有 TLB 未命中时才需要进行页表遍历。页表无法完成访问时可能触发 page fault，但原因不只包括“页面不在物理内存”：懒分配、写时复制、文件页尚未载入、权限冲突和非法地址都可能产生 page fault。内核能够处理的就补上映射或权限状态；无法处理的用户态访问通常会转化为 `SIGSEGV` 等信号。[^page-tables]

## PC 很重要，但不是全部

程序计数器是理解执行流的好工具。在 x86-64 中，它通常称为 `RIP`。从体系结构角度看，它指示下一步要执行的指令位置，执行流可以因为顺序执行、条件或无条件分支、函数调用与返回、系统调用、异常和硬件中断等原因改变。

不过，不能把真实 CPU 想象成永远严格执行“取一条、执行一条、PC 加一”。x86 指令长度不固定，正常顺序执行要前进当前指令的长度；现代处理器内部还有流水线、乱序执行、推测执行和多级缓存。PC 仍然是架构层面观察控制流的重要状态，但它不是解释整个系统行为的唯一变量。

进程调度也不是简单地“时钟芯片每隔几毫秒中断一次，然后内核只替换 PC”。现代系统可能使用每 CPU 定时器和 tickless 配置；任务阻塞、唤醒、主动让出或优先级变化也能触发调度。上下文切换需要保存和恢复的不只是指令指针，还包括通用寄存器、栈、地址空间以及其他架构状态。Linux 甚至可以在空闲 CPU 或只有单个可运行任务的 CPU 上省略周期性的 scheduling tick。[^nohz]

## 把整条链重新串起来

现在可以给出一个更接近真实系统、同时仍然适合作为心智模型的版本：

1. 我按下 `l`、`s` 和回车，操作系统输入栈把按键事件交给终端模拟器。
2. 终端模拟器把相应字节写入 PTY master，Shell 的行编辑器从 PTY slave 读取并编辑命令行。
3. Shell 接受命令行，进行解析、展开和命令查找，确认这里的 `ls` 是一个外部程序。
4. Shell 建立独立执行环境；在经典模型里，它创建子进程，并让子进程调用 `execve`。父 Shell 负责等待和作业控制。
5. `execve` 进入内核并替换子进程的程序映像。内核验证 ELF，建立虚拟内存映射，准备动态加载器、参数、环境变量和初始栈。
6. 动态加载器和运行时完成初始化，随后进入 `ls`。代码页和数据页可以按需进入物理内存。
7. `ls` 多次请求内核读取目录项和必要的文件元数据，在用户态完成排序与格式化，再把结果写入标准输出。
8. 输出字节沿 PTY 回到终端模拟器，由终端模拟器解析并渲染成屏幕上的文件名。
9. `ls` 退出，父 Shell 收集它的退出状态，再打印下一次提示符。

在这条链里，键盘输入始终是数据，不会凭空生成新的 CPU 指令；但数据会经过多个进程和内核子系统，并影响既有程序的控制流。用户态代码也确实可以在不执行内核代码的情况下做大量计算，只是像 `ls` 这样的文件系统工具远比“最后调用一次 `write`”更依赖内核。

Bash 和 Zsh 共享这套 Unix 式的大框架，但它们的解析、展开、builtin、作业控制和交互功能并不只是表面语法糖。理解共同的操作系统机制很有价值，同时也要给平台、CPU 架构和具体 Shell 的差异留下位置。

这一次，真正串起整条链的不只是 PC，而是四个彼此配合的概念：**进程执行环境、用户态与内核态的受控切换、虚拟内存映射，以及终端模拟器与 PTY 的数据通路。**

## 参考资料

[^mac-shell]: Apple Support, [Change the default shell in Terminal on Mac](https://support.apple.com/guide/terminal/change-the-default-shell-trml113/mac).

[^apple-silicon]: Apple Developer, [Porting your macOS apps to Apple silicon](https://developer.apple.com/documentation/apple-silicon/porting-your-macos-apps-to-apple-silicon).

[^macho]: Apple Developer, [Overview of the Mach-O Executable Format](https://developer.apple.com/library/archive/documentation/Performance/Conceptual/CodeFootprint/Articles/MachOOverview.html).

[^pty]: Linux man-pages, [pty(7)](https://man7.org/linux/man-pages/man7/pty.7.html).

[^tty-ldisc]: Linux Kernel Documentation, [TTY Line Discipline](https://www.kernel.org/doc/html/latest/driver-api/tty/tty_ldisc.html)；另见 [N_TTY](https://www.kernel.org/doc/html/latest/driver-api/tty/n_tty.html).

[^posix-terminal]: The Open Group, [General Terminal Interface](https://pubs.opengroup.org/onlinepubs/9699919799/basedefs/V1_chap11.html).

[^readline]: GNU Bash Reference Manual, [Readline Interaction](https://www.gnu.org/software/bash/manual/html_node/Readline-Interaction.html).

[^bash-search]: GNU Bash Reference Manual, [Command Search and Execution](https://www.gnu.org/software/bash/manual/html_node/Command-Search-and-Execution.html).

[^bash-env]: GNU Bash Reference Manual, [Command Execution Environment](https://www.gnu.org/software/bash/manual/html_node/Command-Execution-Environment.html).

[^fork]: Linux man-pages, [fork(2)](https://man7.org/linux/man-pages/man2/fork.2.html).

[^execve]: Linux man-pages, [execve(2)](https://man7.org/linux/man-pages/man2/execve.2.html).

[^linux-syscall-table]: Linux kernel source, [x86-64 system call table](https://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git/tree/arch/x86/entry/syscalls/syscall_64.tbl).

[^intel-sdm]: Intel, [Intel 64 and IA-32 Architectures Software Developer's Manuals](https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html).

[^mm-concepts]: Linux Kernel Documentation, [Memory Management Concepts Overview](https://www.kernel.org/doc/html/latest/admin-guide/mm/concepts.html).

[^getdents]: Linux man-pages, [getdents(2)](https://man7.org/linux/man-pages/man2/getdents.2.html).

[^ls-color]: GNU Coreutils Manual, [General output formatting](https://www.gnu.org/software/coreutils/manual/html_node/General-output-formatting.html).

[^page-tables]: Linux Kernel Documentation, [Page Tables](https://www.kernel.org/doc/html/latest/mm/page_tables.html).

[^nohz]: Linux Kernel Documentation, [NO_HZ: Reducing Scheduling-Clock Ticks](https://www.kernel.org/doc/html/latest/timers/no_hz.html).
