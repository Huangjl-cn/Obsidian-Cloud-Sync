# 从 `ls` 到屏幕上的文件名：一条命令如何穿过终端、Shell 与内核

换到 Mac 之后，我最先注意到的是 Shell 变了：Ubuntu 上常见的是 Bash，macOS 默认的是 Zsh。[^mac-shell] 日常使用几乎没有停顿，`cd`、`ls`、管道和重定向依然熟悉。真正勾起我好奇心的，是这些命令背后的过程。

在终端里输入 `ls`，回车，文件名几乎立刻出现。短短一瞬间，终端模拟器、Shell、C 运行库、操作系统内核、文件系统和 CPU 已经完成了一次配合。

为了让每个底层细节都有明确落点，本文以 **x86-64 Linux、GUI 终端模拟器、交互式 Shell 和 GNU `ls`** 为例。macOS 上的高层结构与之相似，但 Darwin/XNU、ARM64 和 Mach-O 带来了另一套系统调用与程序加载细节。[^apple-silicon] [^macho]

## 先看清“谁在执行”

接下来会频繁出现“终端模拟器接收按键”“Shell 解析命令”“内核读取文件”这样的句子。这是一种叙述上的压缩。在物理层面，真正推进计算的是 CPU 的逻辑核心：它根据指令指针指向的位置执行机器指令，再由这些指令读取和修改寄存器、内存及设备状态。

每个逻辑核心都有自己的指令指针。在 x86-64 中，这个寄存器叫 `RIP`。多核机器上的不同核心可以在同一时刻分别执行终端模拟器、Shell、`ls` 或内核中的指令；同一个核心也会随着系统调用、中断和调度，在不同执行上下文之间切换。

Shell 是一个用户态进程。它的虚拟地址空间里有来自可执行文件和共享库的机器指令，也有堆、栈和其他会持续变化的数据。内核同样由机器指令与数据结构组成，只是运行在受保护的内核执行环境中。普通编译程序的代码页通常以只读方式映射，具体页面可以按需进入或离开物理内存；程序的输入、状态和数据则会在运行中不断变化。[^mm-concepts]

因此，“Shell 解析 `ls`”展开来说，就是某个 CPU 核心正在执行 Shell 进程地址空间中的既有指令，这些指令读取命令字符串，并根据字符串内容选择后续控制流。“内核读取目录”则表示 CPU 已经通过受控入口转入内核执行环境，开始执行文件系统相关的内核指令。进程阻塞时，没有核心在替它推进指令，但它的内存和进程状态仍被系统保存着。

键盘输入在这套模型中是数据。它影响已有指令的分支、参数和处理结果，而一条普通的 Shell 命令并不会临时生成一套全新的机器指令。

## 命令行窗口里有两个程序

一个终端窗口里至少有两个重要角色。

第一个是终端模拟器，例如 GNOME Terminal、Konsole 或其他终端应用。它管理窗口、字体、颜色、光标和键盘事件。第二个是 Shell，例如 Bash 或 Zsh。Shell 负责显示提示符、读取命令行、解析语法和组织其他程序运行。

它们都是普通的用户态进程，通常通过一对伪终端，也就是 PTY，进行通信：

```text
键盘事件 -> 终端模拟器 -> PTY master -> PTY slave -> Shell
Shell/ls -> PTY slave -> PTY master -> 终端模拟器 -> 屏幕
```

PTY 是一对相连的虚拟字符设备。终端模拟器持有 master 端，Shell 的标准输入、标准输出和标准错误通常连接到 slave 端。写入 master 的字节会成为 slave 一侧的输入，写入 slave 的内容也会从 master 一侧读出。[^pty]

Linux 内核在这条通路中提供 TTY 层与 line discipline，处理终端输入队列、特殊控制字符和 canonical mode 等行为。[^tty-ldisc]

当我按下 `l` 时，操作系统输入栈把按键事件交给当前获得焦点的终端窗口。终端模拟器结合键盘布局、修饰键和输入法生成相应字节，再写入 PTY master。方向键会生成转义序列，中文输入法则可能一次提交多个 Unicode 字符对应的编码字节。

Shell 一侧通常还有 Readline 或 ZLE 这样的行编辑器。它负责历史记录、光标移动、删除、补全和快捷键，并可以逐字符处理 PTY 中到达的数据。终端处于 canonical mode 时，TTY 接口以行为单位组织输入；行编辑器也可以使用 noncanonical mode 自己完成编辑。[^posix-terminal] [^readline]

回车被行编辑器接受后，字符串 `ls` 正式交给 Shell。

## `ls` 首先只是一段文本

Shell 收到命令行后，会先进行词法分析和语法解析。引号、变量展开、通配符、命令替换、管道、重定向和控制运算符，都在这一阶段获得含义。

随后，Shell 查找命令名。一个名字可能对应 alias、Shell 函数、builtin，或者 `$PATH` 中的外部可执行文件。Bash 会按照自己的查找规则处理这些候选项，并缓存已经找到的外部程序路径。[^bash-search]

可以直接询问当前 Shell 如何理解 `ls`：

```sh
type ls
command -V ls
```

`cd` 展示了 builtin 的价值。进程的当前工作目录属于进程自身状态，因此 `cd` 需要在当前 Shell 的执行环境里修改它。Shell 函数和 builtin 可以影响这个环境，另一个独立进程中的目录变化则只属于那个进程。

接下来假设当前环境里没有同名 alias 或函数，`ls` 被解析为 `$PATH` 中的外部程序。Shell 开始为它准备参数、环境变量、文件描述符、当前目录和重定向。Bash 文档把外部命令所处的这组状态称为 separate execution environment。[^bash-env]

## Shell 为 `ls` 留出一个新进程

经典的 Unix 执行模型分成两步：先创建子进程，再让子进程换上一套新的程序映像。

```text
父 Shell --fork/spawn--> 子进程 --execve--> ls 程序映像
          \
           `--等待前台任务，维护作业状态
```

具体实现可以选用 `fork`、`vfork`、`clone` 或 `posix_spawn` 一类机制。站在 Shell 语义层面，前台外部命令获得独立执行环境，父 Shell 保留原有状态，并负责作业控制和等待命令结束。

在经典的 `fork` 模型中，子进程刚创建时继承父进程的大量状态。随后，子进程调用 `execve`，把自己的程序映像替换为 `ls`。`execve` 保留进程身份和一部分属性，同时重建代码、数据、栈等程序状态；成功后，执行流直接进入新程序，原调用点也就结束了使命。[^fork] [^execve]

父 Shell 仍在另一个进程中等待。等 `ls` 退出，它会收集退出状态、更新作业信息，然后打印下一次提示符。

## 从 libc 走进内核

Shell 通常通过 libc 提供的 `execve()` 包装函数发起请求。继续沿用 Linux x86-64 这一限定：原生系统调用 ABI 把系统调用号放进 `rax`，前三个参数放进 `rdi`、`rsi`、`rdx`，而 `execve` 的系统调用号是 59。底层包装代码随后执行 `syscall` 指令。[^linux-syscall-table]

这组数字与寄存器约定属于 Linux x86-64 ABI。Linux ARM64、macOS ARM64 和其他平台各自拥有对应的 ABI。

CPU 执行 `syscall` 时，会根据模型专用寄存器中预先配置的入口信息转入内核代码，并更新执行权限和状态。x86-64 的这条指令把下一条用户态指令的地址保存到 `RCX`，把标志保存到 `R11`，再让 `RIP` 指向内核入口。进入内核后，Linux 的入口代码继续切换到内核栈、保存上下文、读取系统调用号，再把请求分派给相应实现。[^intel-sdm]

从控制流角度看，这次切换最直观的主线就是 `RIP` 从用户代码转向内核入口；从保护机制角度看，它还同时改变了 CPU 的特权执行状态，并由内核入口代码建立后续执行所需的栈和寄存器上下文。单独把 `RIP` 改成一个内核地址，并不会赋予用户态代码执行内核指令的权限。

因此，用户态进入内核态可以概括为：**CPU 完成了一次受硬件约束的控制流与权限上下文切换，`RIP` 的改变是其中最直观的表现。** 它和普通函数跳转的关键差别，正是这套受保护的权限转换与入口规则。

此时，`execve` 的路径、参数数组和环境变量数组仍然位于用户空间。内核把它们视为需要验证的用户地址，在权限和边界检查之后读取所需内容。

## 内核搭起新的程序映像

内核首先沿文件系统解析可执行文件路径，检查执行权限并识别文件格式。对于常见的动态链接 ELF 程序，ELF program headers 描述了各个段应当映射到哪些虚拟地址，以及它们需要的读、写、执行权限。

接下来，内核为进程建立新的虚拟地址空间布局，其中包括可执行文件的代码段和数据段、初始栈、命令行参数、环境变量以及 auxiliary vector。ELF 中的 `PT_INTERP` 还会指定动态加载器，由内核把它一同纳入新映像。

这一阶段的核心动作是建立虚拟内存区域与文件之间的映射。可执行文件页面可以由页缓存支持，并在首次访问时通过 demand paging 进入物理内存。各个虚拟页对应的物理页可以分布在不同位置；已经存在于页缓存中的文件内容也可以直接复用。[^mm-concepts]

内核完成初始执行上下文后，返回用户态，新程序从 ELF 入口开始。对动态链接程序来说，这个入口通常先把执行权交给动态加载器。加载器映射所需共享库、处理重定位和符号解析，再进入 C 运行时启动代码，最终调用 `ls` 的 `main`。

从 Shell 的 `execve()` 调用到 `ls` 的 `main`，中间已经跨过系统调用入口、ELF 加载、虚拟内存映射和动态链接几个层次。

## `ls` 开始读取目录

来到 `main` 以后，`ls` 解析命令行参数，并确定目标目录和输出格式。真正的目录数据由内核中的文件系统提供，因此执行过程会在用户态计算和内核服务之间多次往返。

一次普通的 Linux 目录列表大致包含这些步骤：

1. 打开目标目录。
2. 通过 `getdents64` 一类系统调用取得目录项。
3. 根据选项、文件系统能力和颜色规则，使用 `stat`、`statx` 一类接口取得额外元数据。
4. 在用户态比较文件名、排序并计算输出布局。
5. 通过 `write` 把结果写入标准输出。
6. 关闭资源并退出进程。

实际调用会随着 GNU Coreutils 版本、命令选项和文件系统而变化。`getdents64` 提供从目录文件描述符读取目录项的内核接口；GNU `ls` 文档也说明，部分颜色规则会让程序为文件获取额外的 `stat` 信息。[^getdents] [^ls-color]

排序和格式化主要发生在用户态。CPU 在当前进程有权限访问的内存里执行比较、跳转和数据搬运指令。打开目录、读取目录项和写出结果，则通过系统调用请求内核操作文件描述符与文件系统对象。

## 字节沿 PTY 回到终端

`ls` 完成一批格式化后，通过标准输出文件描述符写出字节。标准输出连接终端时，这些字节进入 PTY slave，并在 master 端等待终端模拟器读取。

其中既有文件名和换行，也可能有 ANSI 转义序列。终端模拟器解析这些数据，更新自己的字符网格、光标和颜色状态，再调用字体与图形系统把字形绘制成像素。至此，文件名才真正出现在屏幕上。

重定向可以让同一条执行链在输出阶段转向另一个目的地：

```sh
ls > files.txt
```

此时 Shell 会在启动 `ls` 前设置好标准输出文件描述符。`ls` 仍然调用 `write`，只是字节最终进入了 `files.txt`，终端模拟器也不会收到这部分输出。

## 执行期间，CPU 仍在处理整个系统

从体系结构角度看，程序计数器记录下一步执行位置。在 x86-64 中，它通常称为 `RIP`。顺序执行、条件分支、函数调用与返回、系统调用、异常和硬件中断都会让执行位置按照各自规则前进或转移。

前文里每一次“执行者”的变化，都能在某个逻辑核心的 `RIP` 上找到控制流线索：它可能从 Shell 或 libc 的代码转到内核入口，也可能在调度后恢复到另一个进程保存的指令位置。与此同时，特权级、栈、通用寄存器和地址空间共同决定了这段代码以什么身份、访问哪些数据来继续执行。

现代 CPU 内部还运行着流水线、乱序执行、推测执行和多级缓存。`RIP` 提供了架构层面的控制流视角，处理器内部则会并行安排许多微操作，在提交结果时维持指令集规定的可观察行为。

内存访问也在持续进行地址翻译。每个进程拥有自己的虚拟地址空间，其中分布着代码、共享库、堆、栈和许多未映射区域。CPU 使用 TLB 缓存近期翻译，在需要时依据页表完成虚拟地址到物理地址的转换。

访问尚未装入的文件页、懒分配页面或写时复制页面时，CPU 会产生 page fault。内核检查原因后，可以装入页面、分配物理页或更新映射。权限冲突和非法地址也会产生 page fault；内核无法满足的用户态访问通常转化为 `SIGSEGV` 等信号。[^page-tables]

调度器则让多个任务共享每个 CPU。当前任务阻塞、其他任务被唤醒、任务主动让出，或者调度时机到来时，内核可以选择另一个任务运行。一次上下文切换会保存和恢复指令指针、通用寄存器、栈、地址空间及其他架构状态。Linux 的 tickless 配置还允许某些 CPU 在空闲或只有一个可运行任务时省去周期性的 scheduling tick。[^nohz]

因此，在 `ls` 运行期间，执行流可能因系统调用、page fault、硬件中断和调度而多次进入内核，也可能暂时切换到别的进程，之后再从保存的上下文继续。

## 把整条链串起来

现在再看一次从按键到屏幕的完整旅程：

1. 操作系统输入栈把 `l`、`s` 和回车对应的按键事件交给终端模拟器。
2. 终端模拟器把编码后的字节写入 PTY master，Shell 的行编辑器从 PTY slave 读取它们。
3. Shell 接受命令行，完成解析、展开与命令查找，找到外部 `ls` 程序。
4. Shell 建立独立执行环境，并在经典模型中创建子进程；父 Shell 保留自身状态并管理前台任务。
5. 子进程通过 `execve` 进入内核，用 `ls` 替换自己的程序映像。
6. 内核解析 ELF、建立虚拟内存布局、准备动态加载器、参数、环境变量和初始栈。
7. 动态加载器和运行时完成初始化，`ls` 随后开始执行；所需页面可以按需进入物理内存。
8. `ls` 请求内核读取目录项和文件元数据，在用户态完成排序与格式化，再把字节写入标准输出。
9. 输出沿 PTY 回到终端模拟器，被解析、排版并渲染成屏幕上的文件名。
10. `ls` 退出，父 Shell 收集退出状态，重新显示提示符。

这条链里没有哪一个角色独自完成全部工作。终端模拟器管理人与命令行之间的界面，Shell 把文本组织成执行任务，内核提供受保护的进程、内存和文件系统服务，CPU 则在不同执行上下文之间不断推进指令。

下一次看到提示符时，我会把它理解成一个已经准备好的协作入口：输入是数据，Shell 赋予它语法，内核赋予程序资源，而终端最终把输出变回人能看到的文字。

## 参考资料

[^mac-shell]: Apple Support, [Change the default shell in Terminal on Mac](https://support.apple.com/guide/terminal/change-the-default-shell-trml113/mac).

[^apple-silicon]: Apple Developer, [Porting your macOS apps to Apple silicon](https://developer.apple.com/documentation/apple-silicon/porting-your-macos-apps-to-apple-silicon).

[^macho]: Apple Developer, [Overview of the Mach-O Executable Format](https://developer.apple.com/library/archive/documentation/Performance/Conceptual/CodeFootprint/Articles/MachOOverview.html).

[^pty]: Linux man-pages, [pty(7)](https://man7.org/linux/man-pages/man7/pty.7.html).

[^tty-ldisc]: Linux Kernel Documentation, [TTY Line Discipline](https://www.kernel.org/doc/html/latest/driver-api/tty/tty_ldisc.html)；另见 [N_TTY](https://www.kernel.org/doc/html/latest/driver-api/tty/n_tty.html).

[^posix-terminal]: The Open Group, [General Terminal Interface](https://pubs.opengroup.org/onlinepubs/9699919799/basedefs/V1_chap11.html).

[^readline]: GNU Bash Reference Manual, [Readline Interaction](https://www.gnu.org/software/bash/manual/html_node/Readline-Interaction.html).

[^bash-search]: GNU Bash Reference Manual, [Command Search and Execution](https://www.gnu.org/software/bash/manual/html_node/Command-Search-and-Execution.html).

[^bash-env]: GNU Bash Reference Manual, [Command Execution Environment](https://www.gnu.org/software/bash/manual/html_node/Command-Execution-Environment.html).

[^fork]: Linux man-pages, [fork(2)](https://man7.org/linux/man-pages/man2/fork.2.html).

[^execve]: Linux man-pages, [execve(2)](https://man7.org/linux/man-pages/man2/execve.2.html).

[^linux-syscall-table]: Linux kernel source, [x86-64 system call table](https://github.com/torvalds/linux/blob/master/arch/x86/entry/syscalls/syscall_64.tbl).

[^intel-sdm]: Intel, [Intel 64 and IA-32 Architectures Software Developer's Manuals](https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html).

[^mm-concepts]: Linux Kernel Documentation, [Memory Management Concepts Overview](https://www.kernel.org/doc/html/latest/admin-guide/mm/concepts.html).

[^getdents]: Linux man-pages, [getdents(2)](https://man7.org/linux/man-pages/man2/getdents.2.html).

[^ls-color]: GNU Coreutils Manual, [General output formatting](https://www.gnu.org/software/coreutils/manual/html_node/General-output-formatting.html).

[^page-tables]: Linux Kernel Documentation, [Page Tables](https://www.kernel.org/doc/html/latest/mm/page_tables.html).

[^nohz]: Linux Kernel Documentation, [NO_HZ: Reducing Scheduling-Clock Ticks](https://www.kernel.org/doc/html/latest/timers/no_hz.html).
